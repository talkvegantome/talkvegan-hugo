{{- define "response" }}
[
    {{- range $index, $element := .Data.Pages }}
      {{ if $index}}, {{ end }}
      {
          "friendlyName": {{ .Title | jsonify}},
          "machineName": {{.Params.machine_name | jsonify}},
          "rawContent": {{.RawContent | jsonify}},
          "relativePermalink": {{.RelPermalink |  jsonify}},
          {{- with .Site.GetPage .Section }}
          "section": {
            "friendlyName": {{.Title |jsonify}},
            "machineName": {{.Params.machine_name | jsonify}}
          },
          {{- end}}
          "permalink" : "{{ .Permalink }}"
      }

    {{- end }}
]
{{- end }}
